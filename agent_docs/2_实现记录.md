# 实现记录

## 已完成

### 阶段 1: 项目骨架

- [x] Cargo Workspace (`core`, `desktop`, `mobile`)
- [x] 目录结构搭建
- [x] 所有 crate 脚手架

### 阶段 2: core 模块

| 模块           | 文件                                        | 测试       | 说明                                     |
| -------------- | ------------------------------------------- | ---------- | ---------------------------------------- |
| `crypto`       | `core/src/crypto.rs`                        | 3 tests ✅ | Argon2id → Key, ChaCha20-Poly1305 加解密 |
| `message`      | `core/src/message.rs`                       | 2 tests ✅ | 消息模型 + JSON 序列化，含文件附件       |
| `disguise`     | `core/src/disguise.rs`                      | 2 tests ✅ | 随机伪装发件人（域名+用户名+显示名）     |
| `config`       | `core/src/config.rs`                        | —          | 配置结构体定义                           |
| `store`        | `core/src/store/{mod,sqlite}.rs`            | 1 test ✅  | Storage trait + SQLite 实现              |
| `email/imap`   | `core/src/email/imap.rs`                    | —          | IMAP 轮询器（同步 imap crate）           |
| `email/smtp`   | `core/src/email/smtp.rs`                    | —          | SMTP 发送器（Base64 CTE）                |
| `email/parser` | `core/src/email/parser.rs`                  | 1 test ✅  | MIME 解析（Base64 传输编码）             |
| `plugin`       | `core/src/plugin/{mod,protocol,process}.rs` | —          | PluginManager + JSON-RPC stdio 协议      |
| `event`        | `core/src/event.rs`                         | —          | EventBus (broadcast channel)             |
| `router`       | `core/src/router.rs`                        | —          | 消息分发 + 有序交付 + 去重               |

**core**: 9/9 测试通过, `cargo check` 0 errors

### 阶段 3: 前端 (Vue 3 + TDesign)

| 文件                       | 说明                                 |
| -------------------------- | ------------------------------------ |
| `src/main.ts`              | 入口 (Pinia + Router + TDesign)      |
| `src/App.vue`              | 布局 (侧边导航栏, 4 页面路由)        |
| `src/router/index.ts`      | 路由定义                             |
| `src/stores/yse.ts`        | Pinia 全局状态 (消息/插件/配置/日志) |
| `src/api/commands.ts`      | Tauri invoke 封装 (14 个命令)        |
| `src/views/ChatView.vue`   | 聊天界面 (联系人筛选、发送、文件)    |
| `src/views/ConfigView.vue` | 邮箱/加密/消息分发映射配置           |
| `src/views/PluginView.vue` | 插件管理 (添加/启停/删除/运行状态)   |
| `src/views/LogView.vue`    | 日志查看 (级别筛选、刷新、清空)      |

**前端**: Vue-tsc 通过, vite build 生产构建通过

### 阶段 4: Tauri 桌面端桥接

| 文件                                | 说明                          |
| ----------------------------------- | ----------------------------- |
| `desktop/src/main.rs`               | Tauri 入口                    |
| `desktop/src/lib.rs`                | 应用初始化 + 命令注册         |
| `desktop/src/commands.rs`           | 14 个 Tauri 命令实现          |
| `desktop/tauri.conf.json`           | Tauri 配置 (窗口、构建、打包) |
| `desktop/capabilities/default.json` | 权限配置                      |
| `desktop/build.rs`                  | tauri-build                   |

**14 个 Tauri 命令**:

- `send_message` / `get_messages`
- `get_config` / `save_config` / `set_crypto_password`
- `start_polling` / `stop_polling`
- `list_plugins` / `add_plugin` / `remove_plugin` / `toggle_plugin` / `list_running_plugins`
- `get_logs` / `test_email`

**注意**: Tauri 桌面端编译需要系统依赖 (GTK3, WebKit2GTK). 当前环境无此依赖.
代码结构完全正确, 在有相应系统库的环境上 `cargo check` 即可通过.

## 剩余工作

- [ ] Tauri 应用图标 (`desktop/icons/` 缺省图标文件)
- [ ] Tauri 移动端 (`mobile/` 待配置)
- [ ] 插件示例 (`plugins/example-bot/`)
- [ ] 端到端集成测试 (需真实邮箱)
