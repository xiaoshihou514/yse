# Generated by gansi create. The `yse` dependency is added from the selected
# local checkout via `cargo add yse --path <checkout>/crates/yse`.
[package]
name = "{{name}}"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
license = "MIT OR Apache-2.0"

# This project is its own workspace: creating it inside another checkout
# (e.g. the Yse repository) must not inherit the parent's workspace members.
[workspace]

[build-dependencies]
cxx-qt-build = "0.9.1"
