# Generated by gansi create. The `yse` dependency is added by `gansi create`
# itself via `cargo add yse` (`--path <checkout>/crates/yse` with `--local`).
[package]
name = "{{name}}"
version = "0.1.0"
edition = "2024"