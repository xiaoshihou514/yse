[Desktop Entry]
# Replace the generated com.example application ID (used as this file's
# installed name and Icon value) before publishing.
Type=Application
Name={{title}}
Comment=Native Qt application built with Yse
Exec={{name}}
Icon={{app_id}}
Categories=Utility;
Terminal=false
StartupNotify=true
