[package]
name = "yse"
version = "0.1.0"
edition = "2024"
description = "Reactive Qt desktop applications from Rust (facade over yse-model and yse-ui)"
license = "MIT OR Apache-2.0"

[dependencies]
yse-model = { version = "0.1.0", path = "../yse-model" }
yse-ui = { version = "0.1.0", path = "../yse-ui" }
